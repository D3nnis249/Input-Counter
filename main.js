function charCount(str) {
  let lng = str.length;
  document.getElementById("charCount").innerHTML = lng;
}

function charCountAnswer(str) {
  let lng = str.length;
  document.getElementById("charCountTwo").innerHTML = lng;
}

function charCountTags(str) {
  let lng = str.length;
  document.getElementById("charCountThree").innerHTML = lng;
}

